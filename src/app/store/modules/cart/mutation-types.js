// For large scale Flux implementations, a common standard is to often define
// mutatio types as constants and to host them all in a separate file
export const UPDATE_CART_ITEMS = "UPDATE_CART_ITEMS";
